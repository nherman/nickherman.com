name: Deploy static site

on:
  push:
    branches:
      - master

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::383580889067:role/GITHUB_ACTIONS_NICKHERMAN.COM
          aws-region: us-east-1

      - name: Sync files to S3
        run: |
          aws s3 sync . s3://nickherman.com \
            --delete \
            --exclude ".git/*" \
            --exclude ".github/*" \
            --exclude "README.md"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id E3LVYHA8LZR4FK \
            --paths "/*"